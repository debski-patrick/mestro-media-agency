export interface NavbarConfigType {
  id: number;
  title: string;
  path: string;
}

export interface ServiceConfigType {
  id: number;
  title: string;
}

export interface WorkConfigType {
  id: number;
  title: string;
  description: string[];
}